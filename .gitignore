# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.venv/
venv/
env/

# Benchmark logs (live test output — appended to on every $0.08+ run)
tests/bench_log.jsonl

# OS / editor
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# Agent harness local state (token metrics, hook/session state — not project data)
.omx/
